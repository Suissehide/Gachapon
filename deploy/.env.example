# ── Build / Registry ──────────────────────────────────────────────────────────
NODE_VERSION=20
NPM_VERSION=10
MIRROR_URL=
APP_IMAGE_NAME=gachapon
CONTAINER_REGISTRY_PREFIX=
VERSION=latest

# ── App URLs ──────────────────────────────────────────────────────────────────
FRONT_URL=https://yourdomain.com
CORS_ORIGIN=https://yourdomain.com

# Traefik hostnames (production compose only)
TRAEFIK_FRONT_HOST=yourdomain.com
TRAEFIK_BACK_HOST=api.yourdomain.com

# ── Frontend build args ───────────────────────────────────────────────────────
VITE_API_URL=https://api.yourdomain.com
VITE_ENVIRONMENT=production
VITE_DISCORD_INVITE_URL=https://discord.gg/2KYXP8tsFc

# ── Backend runtime ───────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info
NODE_CLUSTER=false
NODE_TLS_REJECT_UNAUTHORIZED=1
NODE_ENV=production

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://gachapon:gachapon@postgres:5432/gachapon
POSTGRES_USER=gachapon
POSTGRES_PASSWORD=gachapon
POSTGRES_DB=gachapon
# External port mapping for postgres (host:container)
POSTGRES_PORT=5432

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_URL=redis://redis:6379

# ── MinIO (external) ──────────────────────────────────────────────────────────
MINIO_ENDPOINT=https://minio.example.com
MINIO_ACCESS_KEY=your_access_key
MINIO_SECRET_KEY=your_secret_key
MINIO_BUCKET=gachapon

# ── JWT (minimum 32 chars) ────────────────────────────────────────────────────
JWT_SECRET=change_me_in_production_at_least_32_chars
JWT_REFRESH_SECRET=change_me_in_production_at_least_32_chars

# ── OAuth ─────────────────────────────────────────────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://api.yourdomain.com/auth/oauth/google/callback
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=https://api.yourdomain.com/auth/oauth/discord/callback

# ── SMTP (emails transactionnels) ─────────────────────────────────────────────
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Gachapon <noreply@gachapon.app>"
# Dev local : Mailpit (docker-compose inclut le service mailpit, UI sur :8025)
MAILPIT_PORT=8026
MAILPIT_SMTP_PORT=1026
# Prod : ex. Resend → SMTP_HOST=smtp.resend.com, SMTP_USER=resend, SMTP_PASS=re_xxx
#
# Dokploy : le service mailpit est derrière le profil « mailpit » et n'est donc
# PAS déployé par défaut. Ne l'active que sur staging, avec
#   COMPOSE_PROFILES=mailpit
# dans les variables de l'app. La prod doit pointer SMTP_HOST vers un vrai
# fournisseur : sans SMTP_HOST/SMTP_PORT le back refuse de démarrer, ce qui est
# voulu — un fallback silencieux vers mailpit retiendrait les emails de
# vérification et de réinitialisation de mot de passe.
# Deux apps Dokploy sur le même hôte ne peuvent pas publier 8025 toutes les deux :
# si tu actives mailpit des deux côtés, donne un MAILPIT_PORT distinct à chacune.
